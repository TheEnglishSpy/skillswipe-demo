import { Badge } from "./ui"; import { Check, Quote } from "lucide-react";
export function ValidatedClaimCard({item}:{item:{domain:string;claim:string;label:string;count:number;quote?:string;source?:string;status:string}}){const valid=item.status==="validated";return <article className="rounded-[28px] border border-[var(--line)] bg-[var(--card)] p-5 shadow-sm md:p-6"><div className="mb-4 flex items-center justify-between gap-3"><Badge tone={valid?"green":"neutral"}>{valid?<><Check size={12}/> Validated</>:"Building evidence"}</Badge><span className="text-xs text-[var(--muted)]">{item.domain}</span></div><h3 className="display text-[25px] font-semibold leading-[1.08]">{item.claim}</h3><div className="mt-4 flex gap-2 text-xs font-semibold"><span className="rounded-full bg-[#f0eee5] px-3 py-1.5">{item.label}</span><span className="rounded-full bg-[#f0eee5] px-3 py-1.5">{item.count} people</span></div>{item.quote&&<blockquote className="mt-5 border-t border-[var(--line)] pt-4 text-sm leading-6 text-[var(--muted)]"><Quote size={16} className="mb-2 text-[var(--accent)]"/>“{item.quote}” <footer className="mt-2 text-xs font-bold text-[var(--ink)]">Approved evidence · {item.source}</footer></blockquote>}</article>}
