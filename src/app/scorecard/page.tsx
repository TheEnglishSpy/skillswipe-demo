import { Header, Badge } from "@/components/ui"; import { noor } from "@/lib/demo-data"; import { BarChart3, Calendar, Layers3, Users } from "lucide-react";
export default function Scorecard(){return <><Header/><main className="mx-auto max-w-5xl px-5 py-10"><Badge tone="neutral">Advanced view</Badge><h1 className="display mt-4 text-5xl md:text-7xl">Evidence scorecard</h1><p className="mt-4 max-w-2xl text-[var(--muted)]">Confidence is calculated per claim and context. It reflects source relevance, specificity, recency and corroboration—never negative ratings.</p><div className="mt-10 space-y-4">{noor.claims.map((x,i)=><article key={x.claim} className="rounded-[26px] border border-[var(--line)] bg-[var(--card)] p-6"><div className="flex flex-col gap-5 md:flex-row md:items-center"><div className="flex-1"><div className="flex items-center gap-2"><Badge tone={x.status==="validated"?"green":"neutral"}>{x.status==="validated"?"Validated":"Building evidence"}</Badge><span className="text-xs text-[var(--muted)]">{x.domain}</span></div><h2 className="display mt-3 text-2xl">{x.claim}</h2></div><div className="grid grid-cols-4 gap-5 text-center md:w-[390px]">{[[BarChart3,i<3?`${78-i*8}%`:"—","Confidence"],[Users,String(x.count),"People"],[Layers3,i<3?"3":"1","Sources"],[Calendar,"Fresh","Recency"]].map(([Icon,n,l])=>{const I=Icon as typeof Users;return <div key={l as string}><I size={15} className="mx-auto text-[var(--muted)]"/><b className="mt-2 block text-sm">{n as string}</b><span className="text-[9px] uppercase tracking-wide text-[var(--muted)]">{l as string}</span></div>})}</div></div>{x.status==="validated"&&<div className="mt-5 h-1.5 overflow-hidden rounded-full bg-black/10"><div className="h-full rounded-full bg-[var(--accent)]" style={{width:`${78-i*8}%`}}/></div>}</article>)}</div><p className="mt-6 text-xs leading-5 text-[var(--muted)]">Miller evidence levels are available to coaches and recruiters as supporting detail. Consumer profiles use plain labels: Recognised, Seen in practice, and Would call for this.</p></main></>}
